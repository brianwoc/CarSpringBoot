CREATE TABLE car(id BIGINT PRIMARY KEY AUTO_INCREMENT,
vin VARCHAR(20), brand VARCHAR (30),model VARCHAR (30),date date ,color VARCHAR  (30));

--
--public enum Color {
--WHITE,
--BLACK,
--RED,
--GREEN,
--BLUE,
--SILVER
--}


--CREATE TABLE car(id BIGINT PRIMARY KEY AUTO_INCREMENT,
--  vin VARCHAR(20), brand VARCHAR (30),model VARCHAR (30),date date ,color VARCHAR (30));